{% extends "layout.twig" %}

{% block title %} {{parent()}} - Classement Championnat{% endblock title %}

{% block container%}
    <div class="span12 well"> 
        <h4 class="text-center">Classement Championnat</h4>
        {{ligue}}
        <hr>
        <br>
        {% for class in classement %}
            {{class.nom_equipe}} | {{class.nb_point_championnat}} 
            <br> 
        {% endfor %}
    </div>
{% endblock container%}